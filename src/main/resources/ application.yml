spring:
  application:
    name: payment-routing-engine

  datasource:
    url: jdbc:postgresql://localhost:5432/payment_routing_db
    username: postgres
    password: 12345
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  security:
    user:
      name: admin
      password: admin123

server:
  port: 8080

jwt:
  secret: fawry-payment-routing-secret-key-2025-mahmoud-project
  expiration: 86400000

logging:
  level:
    com.fawry.paymentrouting: DEBUG
    org.hibernate.SQL: DEBUG